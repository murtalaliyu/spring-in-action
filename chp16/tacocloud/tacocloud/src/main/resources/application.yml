security:
  user:
    name: buzz
    password: infinity

taco:
  orders:
    pageSize: 10

discount:
  codes:
    abcdef: 10

spring:
  data:
    rest:
      base-path: /api
  datasource:
    generate-unique-name: false
    name: tacocloud
    username: user
    password: pass
  application:
    name: taco-cloud
  boot:
    admin:
      client:
        url: http://localhost:9090
        username: user
        password: 56c90241-007a-4119-9259-b91fe8ba67e8

management:
  endpoints:
    web:
      #base-path: /management (default will be /actuator)
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
  info:
    git:
      mode: full

info:
  contact:
    email: support@tacocloud.com
    phone: 123-456-7890

---

spring:
  profiles: prod

  datasource:
    url: jdbc:mysql://localhost/tacocloud
    username: tacouser
    password: tacopassword

  logging:
    level:
      tacos: WARN
