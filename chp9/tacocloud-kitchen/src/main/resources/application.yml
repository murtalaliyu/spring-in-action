server:
  port: 8081

---

spring:
  profiles: jms-template, jms-listener

  # The values given here are actually the default values. But they are actually
  # set here as an example of setting the Artemis properties.

  jms:
    template:
      receive-timeout: 2

  artemis:
    host: localhost
    port: 64686
    user: tacoweb
    password: l3tm3in
    embedded:
      enabled: false
